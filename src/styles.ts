import { css } from '@linaria/core'
import { styled } from '@linaria/react'

export const ElFeature = styled.div`
  display: flex;
  align-items: center;
  gap: var(--spacing-1);
`
export const elIcon = css`
  width: var(--icon-sm);
  height: var(--icon-sm);
`

export const elForm = css`
 display: flex;
 gap: 20px;
 flex-direction: column;
`